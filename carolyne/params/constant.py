# Constants that are true of every machine, whatever the ISA or the
# configuration. A number belongs here only when NO description and NO config
# could ever state it differently.
#
# Imports nothing, names no ISA — both planes may read it.

BYTE_WIDTH = 8          # bits in a byte
