# The µop contract — the only boundary between an ISA package and the generic
# OoO engine. An ISA package hands the engine an IsaDescription (elaboration
# plane); at run time the two sides speak exclusively in µop records (hardware
# plane). Normative spec: docs/design/uop_contract.md.
