/* Generated by compile_tool/cheader.py from the machine's own MemoryLayout.
 * Do not edit this copy — edit compile_tool/template/carolyne_io.h.in and
 * rebuild, or the header and the hardware stop agreeing. */

#ifndef CAROLYNE_IO_H
#define CAROLYNE_IO_H

/* --- the machine ---------------------------------------------------------- */
#define CAROLYNE_IMEM_BASE   @IMEM_BASE@u
#define CAROLYNE_IMEM_BYTES  @IMEM_BYTES@u
#define CAROLYNE_DMEM_BASE   @DMEM_BASE@u
#define CAROLYNE_DMEM_BYTES  @DATA_BYTES@u
#define CAROLYNE_STACK_TOP   @STACK_TOP@u

/* --- the I/O words -------------------------------------------------------- */
@DOORS@

/* --- what a program calls ------------------------------------------------- */

/* Write one character. */
static inline void print_char(char value)
{
    CAROLYNE_PUTCHAR = (unsigned int)(unsigned char)value;
}

/* Write one signed integer, in decimal. */
static inline void print_int(int value)
{
    CAROLYNE_PUTINT = (unsigned int)value;
}

/* Write a NUL-terminated string. */
static inline void print_str(const char *text)
{
    while (*text) {
        print_char(*text++);
    }
}

/* Stop the machine. Never returns. */
static inline void finish(int code)
{
    CAROLYNE_EXIT = (unsigned int)code;
    for (;;) { }
}

#endif /* CAROLYNE_IO_H */
